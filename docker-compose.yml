services:
  minfo:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
    # If you want to use server path input, mount your media folder:
    # volumes:
    #   - ./media:/media:ro
