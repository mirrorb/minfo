services:
  minfo:
    build: .
    privileged: true
    ports:
      - "28081:8080"
    environment:
      PORT: "8080"
      WEB_PASSWORD: "adminadmin"
      REQUEST_TIMEOUT: "20m"
    volumes:
      - /home/mirrorb/qbittorrent/Downloads:/media:ro
